import 'package:flutter/material.dart';

class FastEntryScreen extends StatelessWidget {
  const FastEntryScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text("Fast Entry")));
}
